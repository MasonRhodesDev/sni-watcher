# Drop-in for waybar.service: ensure the standalone watcher is up first.
# Install as: ~/.config/systemd/user/waybar.service.d/sni-watcher.conf
[Unit]
After=sni-watcher.service
Wants=sni-watcher.service
